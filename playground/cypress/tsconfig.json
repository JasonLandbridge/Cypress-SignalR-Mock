{
	"extends": "../tsconfig.json", // Needed for the path aliases
	"compilerOptions": {
		"types": ["cypress", "node", "cypress-signalr-mock"],
		"lib": ["es5", "dom"],
		"baseUrl": "../",
		"sourceMap": false,
		"target": "ES2019" // Needs to have this target otherwise nullish coalescing operator (??) will not work
	},
	"include": ["**/*.ts"]
}
